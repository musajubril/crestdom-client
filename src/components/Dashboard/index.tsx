import React from 'react';
import Layout from './../Layout';

const Dashboard = () => {
    return (
        <Layout page="Dashboard">
        Dashboard
    </Layout>
    );
}

export default Dashboard;
